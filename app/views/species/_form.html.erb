<%= form_for(@species) do |f| %>
  <% if @species.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@species.errors.count, "error") %> prohibited this species from being saved:</h2>

      <ul>
      <% @species.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :taxonomy_id %><br>
    <%= f.select :taxonomy_id, options_from_collection_for_select(Taxonomy.all, :id, :genus) %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

<%= form_for(@subspecies) do |f| %>
  <% if @subspecies.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@subspecies.errors.count, "error") %> prohibited this species from being saved:</h2>

      <ul>
      <% @subspecies.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :species_id %><br>
    <%= f.select :species_id, options_from_collection_for_select(Species.all, :id, :name) %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
